# Feature acceptance matrix

This matrix owns the 20-feature program scope. A row is complete only when its data migration/round-trip, desktop and 390 px behavior, keyboard/screen-reader contract, Node coverage, browser coverage, documentation, and rollback/recovery point all pass. Derived indexes are never backup authority.

| # | Feature | Phase | Source surfaces | Data change / authority | Desktop, mobile, keyboard, and accessibility contract | Automated proof | Explicit exit criteria |
| ---: | --- | ---: | --- | --- | --- | --- | --- |
| 1 | Local revision history | 1 | `core/storage.js`, `core/database.js`, new `core/revision-store.js`, `components/editor.js`, new `components/history-view.js` | Namespaced immutable revision records plus SHA-256 content/metadata blobs; bounded retention config; Note Markdown/JSON remains authority | Chronological diff/preview dialog; compact stacked mobile view; keyboard selection, trapped focus, Restore and Restore as Copy labels/status | Node: hash dedup, capture boundaries, retention/GC, quota/fallback, restore ordering. Browser: history dialog, focus, mobile, large image, persistence errors | Identical bodies store once; every destructive path has a durable pre-change revision; restore creates a safety revision; fallback is visibly unavailable; all data/test/build/audit gates pass |
| 2 | Rename-safe wikilinks | 2 | `core/note.js`, `core/migrations.js`, `core/database.js`, `utils/wikilinks.js`, `utils/markdown.js`, `components/editor.js`, graph/palette | Add normalized `aliases` array through the next sequential schema migration; backup preserves IDs/aliases; source Markdown links remain authority | Previewed confirmation lists affected notes/links; keyboard-operable repair report; mobile dialog wraps without hiding actions; completion announced | Node: parser ranges/exclusions, collisions, migration, atomic rewrite/rollback, display/fragment fixed points. Browser: rename, autocomplete, backlinks, graph, ambiguity UI | Old title becomes alias; exact inbound targets rewrite atomically while display/fragment text and code stay unchanged; ambiguous imports block; revisions and lossless backup pass |
| 3 | Saved searches | 3 | `utils/search-query.js`, `components/note-list.js`, `components/command-palette.js`, settings/config UI | `savedSearches` config records with stable ID, name, icon, query, sort mode, optional tag, and order; normalizer rejects malformed records | Sidebar/palette create, rename, run, reorder, delete; keyboard reorder alternative and announced result count; usable in off-canvas mobile sidebar | Node: config normalization, malformed queries, ordering, Archive filters. Browser: CRUD, palette run, keyboard reorder, reload, empty states, 390 px | Saved views survive reload exactly, cannot corrupt active query state, share Archive/Trash semantics, and expose full keyboard/mobile control with passing gates |
| 4 | Idempotent Daily notes | 4 | `app/templates.js`, `app/main.js`, `core/database.js`, command palette, calendar entry point | No hidden content format; local `YYYY-MM-DD` title and Daily template Markdown are authority; no duplicate live titles | One Open Today's Note command/shortcut; restores or explicitly offers trashed match; mobile/palette access; result/status announced | Node: local date/DST boundaries, resolution states, duplicate prevention. Browser: repeat command, reload, trashed note, default template, 390 px | Repeated invocation opens one live note, never duplicates; local date is correct across boundaries; Trash choice is explicit; persistence completes before success is reported |
| 5 | Quick Capture | 4 | `app/main.js`, new capture modal, `components/modal.js`, `components/editor.js`, `utils/image.js`, manifest/PWA | Captured text/URL/image becomes destination Markdown through normal queue; allowlisted GET share parameters only; optional capture preferences in config | Focus-trapped text/URL/clipboard/image dialog; destination picker; clear errors and saved status; mobile share-target flow with in-app binary fallback | Node: payload allowlist, URL/size validation, Markdown insertion. Browser: each capture type, arbitrary destination, clipboard denial, share URL clearing, focus/mobile | Capture flushes exact destination without losing open-editor input; unsupported permissions/platforms degrade visibly; URL is cleared after one intake; no POST/binary share claim |
| 6 | Heading outline | 2 | `utils/blocks.js`, wikilink/heading parser, `components/block-editor.js`, new outline component, palette heading search | Derived occurrence-aware heading anchors only; Markdown stays authority and anchors are excluded from backup | Desktop outline and collapsible mobile sheet; active heading state; arrows/Home/End and Enter jump; focus remains meaningful after rerender | Node: fenced-code exclusion, H1-H6, duplicate anchors, Unicode. Browser: click/jump, scroll tracking, rerender, keyboard, 390 px | Every rendered heading has a deterministic unique target; jumps survive editor rerender; no code false positives; outline is keyboard/screen-reader operable |
| 7 | Find and replace | 3 | `components/block-editor.js`, `components/editor.js`, new pure replace utility/view, `core/database.js`, revision store | Source Markdown edits only; current-note UI state transient; vault-wide apply uses preview plan and pre-change revisions | `Ctrl/⌘+F`, next/previous, case/whole-word, count; mobile stacked controls; vault plan requires explicit Apply and announces summary | Node: literal escaping, whole-word/Unicode, source fixed points, plan/revision/partial failure. Browser: selection/caret, undo, preview, keyboard/mobile | Rendered HTML is never mutated; replacement preserves Markdown and selection; vault-wide apply reports changed/unchanged/skipped/failed and is recoverable |
| 8 | Archive | 3 | `core/note.js`, `core/migrations.js`, `core/database.js`, note list/tree, graph, palette, autocomplete, new Archive view | Add nullable `archivedAt` through sequential migration; portable backup/import preserves it; Trash remains separate | Archive/Unarchive commands and labelled Archive list; keyboard open/restore; mobile empty/list states; status not color-only | Node: migration, query scopes, archived parent/child tree, title resolution. Browser: archive views, search `is:archived`, graph/task exclusion, restore | Normal lists/graph/tasks/autocomplete exclude Archive; explicit scope finds it; children are not lost; backup round-trip and all interaction gates pass |
| 9 | Contextual backlinks | 2 | `utils/wikilinks.js`, `core/database.js`, `components/editor.js`, new link-context index | Derived backlink occurrence records with source range, heading, and snippet; never stored as authority | Backlink list shows sanitized heading/snippet and source link; wraps on mobile; keyboard navigation and descriptive accessible names | Node: occurrence/context extraction, code/escape exclusion, sanitizer. Browser: multiple contexts, alias target, navigation, mobile | Every valid inbound occurrence is represented without XSS or code false positives; opening lands at context when available; index updates after durable save |
| 10 | Unlinked mentions | 2 | Shared title/alias index and parser exclusions, new mention index/view, source Markdown converter, revision store | Derived mention records; conversion writes exact source Markdown and revision; no stored inference in backup | Previewed Convert to Wikilink per occurrence/batch; keyboard selection; mobile list; ambiguity and completion announced | Node: title boundaries, code/URL/link/self exclusions, alias ambiguity, incremental update. Browser: preview/convert/undo, large vault, 390 px | Scanner excludes existing links/code/URLs/self, does not guess ambiguous targets, meets 1,000-note budget, and every conversion is previewed/recoverable |
| 11 | Task dashboard | 4 | `utils/blocks.js`, new task index/dashboard, `core/database.js`, block editor checkbox, search filters | Markdown task lines plus terminal `@due(YYYY-MM-DD)` are authority; derived task index excluded from backup | Today/Overdue/Upcoming/No Date/completed groups; native task controls; mobile agenda; saved/failed status announced | Node: due fixed points, exact occurrence/hash, Archive/Trash filters, local dates. Browser: grouping, duplicate-line toggle, source open, keyboard/mobile | Toggle edits only the verified source occurrence, survives reload/history, date groups are locally correct, and all hidden-state/a11y/performance gates pass |
| 12 | Note properties | 5 | New `utils/frontmatter.js`, `utils/blocks.js`, editor/properties view, search query, migrations/import/backup | Leading YAML frontmatter is Markdown authority; supports typed values, `aliases`, immutable `noteforge_id`, and unknown keys; legacy alias metadata migrates once | Labelled typed form with raw-YAML error access; stacked mobile layout; invalid/unsafe fields associated and announced | Node: byte-exact unchanged YAML/body, targeted edits, malformed/unknown/duplicate keys, unsafe URLs, search, backup. Browser: edit/error/focus/mobile | Unchanged Markdown is byte-exact; unknown keys/comments survive; invalid YAML is never hidden/overwritten; dependency license/bundle/CSP/audit gates are recorded |
| 13 | Calendar | 4 | Daily resolver, `utils/blocks.js`, task/date indexes, new calendar component, navigation | Derived from Daily titles, `@date()` blocks, and task `@due()` markers; calendar tuples use local dates | Keyboard month/week grid and equivalent mobile agenda; empty day opens/creates Daily note; source links are labelled | Node: aggregation, leap/DST/timezone boundaries, Archive/Trash filters. Browser: period navigation, empty day, source open, 200%/390 px | Month/week and agenda contain identical items, dates never shift through UTC conversion, empty-day action is idempotent, and keyboard grid pattern passes |
| 14 | Web clipper | 6 | `app/main.js` capture route, bookmarklet generator, capture modal, frontmatter/source URL, sanitizer/CSP | Allowlisted, size-bounded title/URL/selection/article intake; source URL stored as Markdown/frontmatter; URL state cleared after intake | Explicit destination and Save; keyboard/focus trap; mobile bookmark/share workflow; large payload directs to clipboard fallback | Node: query/bookmarklet encoding, limits, unsafe markup/URL. Browser: real capture route, one-shot clearing, sanitizer/CSP, error/mobile | No query auto-saves; content is text until sanitized rendering; replay and oversized payloads are prevented; saved note retains source and passes XSS/offline gates |
| 15 | Back, forward, and recents | 2 | Central `App.openNote`, new navigation reducer/controls, palette, persisted config | Session history up to 100 entries; persisted 50 unique live recent IDs; note IDs only, derived and repairable | Labelled Back/Forward controls/shortcuts and palette recents; mobile top-bar placement; disabled state exposed | Node: reducer, dedupe, origins, Trash/reload normalization. Browser: user/programmatic opens, replay, restore, reload, mobile | One canonical history model avoids duplicate replay entries, prunes invalid IDs, never equates edit time with recency, and flushes before navigation |
| 16 | Tabs and split view | 6 | `app/main.js`, `components/editor.js`, `block-editor.js`, new workspace/tabs components, CSS/config | Versioned workspace IDs/layout/scroll only; max 20 tabs; note ID globally unique across panes; no unsaved content in config | WAI tabs, keyboard close/reorder/cycle/move, focusable splitter, desktop two panes, mobile single-pane collapse retaining state | Node: state normalization, ownership/history reducer. Browser: two-editor races, flush failure, reload, tab/split keyboard, reduced motion, 390 px collapse | Same note never has two writers; every handoff durably flushes; corrupt/deleted IDs recover; mobile collapse loses no tab/pane state; a11y gates pass |
| 17 | Bulk note actions | 3 | `components/note-list.js`, tree/virtualization, `core/database.js` batch API, export/vault helpers, revision store | Selection is ID-based transient state outside DOM; batch tag/archive/reparent/export/Trash uses planned atomic/structured operations | Keyboard range select and action bar; drag has keyboard alternative; touch-safe 390 px controls; destructive confirmation and live summary | Node: selection reducer, cycles, batch plan, rollback/partial report. Browser: virtualized selection, each action, confirmation, focus/status/mobile | Off-screen selection remains exact, parent cycles reject before writes, destructive changes have revisions/confirmation, and results enumerate every selected ID outcome |
| 18 | Backup center | 1 | `core/storage.js`, `core/database.js`, migrations, new backup service/view, `utils/download.js`, replace current JSON controls | Versioned backup envelope contains schema version, all live/Trash notes with IDs/metadata, raw config, integrity hash; local snapshots separate | Storage health and Verify/Restore Preview/Download actions; progress/errors announced; keyboard/focus trap; mobile cards | Node: envelope fixed point, hash validation, future/malformed rejection, quota/fallback, preview/restore. Browser: pending-edit flush, download verify, restore, mobile | A downloaded backup restores IDs, Markdown, images, banners, tags, hierarchy, Trash, settings, and schema exactly; merge import remains separately labelled; local history is not called portable |
| 19 | Folder import and reconciliation | 6 | `utils/vault.js`, new `utils/vault-import.js`, frontmatter IDs, reconciliation UI, database batch/revisions | `noteforge_id` identity; deterministic Add/Update/Conflict/Unchanged plan; no implicit deletes; downloadable report | Preview wizard with per-item decisions; keyboard and file-input fallback; progress/status; readable 390 px diffs | Node: identity precedence, paths/collisions, plan determinism, stale hash, idempotence/interruption. Browser: mocked directory APIs, conflicts, backup gate, fallback/mobile | Scan is read-only; uncertain matches conflict; verified portable backup and revisions precede apply; rerun is idempotent; unsupported APIs never partially write |
| 20 | Block links and transclusion | 5 | `utils/blocks.js`, `utils/wikilinks.js`, `utils/markdown.js`, fragment resolver, block editor, database | Stable trailing `^identifier` markers in Markdown; `[[Note#Heading]]`, `[[Note#^id]]`, `![[Note#^id]]`; derived render cache only | Copy Block Link and keyboard navigation; read-only labelled transclusions; cycle/depth placeholder; nested content fits mobile | Node: ID validation/uniqueness/mutations, parser fixed points, rename/fragments, cycles/depth/XSS/export. Browser: copy/open/transclude, nested/mobile/keyboard | IDs survive supported block edits/moves, duplicate IDs are diagnosed, navigation is exact, transclusion is sanitized/read-only and terminates all cycles within depth 5 |

## Cross-cutting release rule

No row exits its phase with failing Node/browser/build/audit checks, unexplained console/request/HTTP errors, an unverified schema migration, missing keyboard/mobile behavior, or an uncommitted rollback path. The final program additionally requires exact-SHA CI and live provenance on both deployment surfaces.
